# 实验脚本

脚本按职责分为四类：

- `experiments/`：运行训练、推理、回测实验；
- `data/`：构建数据面板和隐藏状态缓存；
- `evaluation/`：生成评估指标；
- `audit/`：执行可复现性和数据完整性审计。

所有脚本都支持 `--help` 查看参数。输出目录建议放在 `results/` 或 `train3/` 下，并在实验记录中保存命令行参数。
