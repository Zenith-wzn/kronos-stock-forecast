# Contributing

Create a focused branch or pull request, keep generated experiment outputs out of source changes, and document the command used to reproduce results. Run the test suite with `pytest` before submitting.
